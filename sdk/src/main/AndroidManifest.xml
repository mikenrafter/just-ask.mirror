<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Merged into every SDK host so getPackageInfo(JUST_ASK_APP_PACKAGE) works under
         Android 11+ package visibility, enabling step-down when Just Ask is installed. -->
    <queries>
        <package android:name="dev.justask.app" />
        <intent>
            <action android:name="dev.justask.action.TRAMPOLINE_PROVIDER" />
        </intent>
    </queries>

</manifest>
